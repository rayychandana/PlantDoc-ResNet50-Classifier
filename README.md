# PlantDoc-ResNet50-Classifier
A deep learning pipeline using a fine-tuned ResNet50 model to classify plant diseases using the public PlantDoc dataset. The model is trained on a GPU and automatically handles class imbalances during evaluation.
